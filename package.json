{
  "name": "IWFSA",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "lint": "find apps -name '*.mjs' -exec node --check {} +",
    "typecheck": "echo 'JavaScript project - no TypeScript type checking required'",
    "test": "node --test apps/api/test/*.test.mjs apps/web/test/*.test.mjs",
    "build": "echo 'Server project - no build step required'"
  }
}